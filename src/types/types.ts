export type GithubUser = {
    name: string;
    bio: string;
};
export type GithubRepo = {
    id: number;
    name: string;
    description: string;
    html_url: string;
    languages_url:string;
};